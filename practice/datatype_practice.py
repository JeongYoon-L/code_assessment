import numpy as np

#Can save other type of data (number, string, object)
list = [0,1,2,3,4,5]
same_list = list(range(5)) #[0,1,2,3,4,5]

#Should have same data type within the array
#Faster than list for large calculation
#More efficient for memory usage
array = np.array([0,1,2,3,4,5])